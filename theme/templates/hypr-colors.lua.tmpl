-- GERADO por `theme` — edite theme/templates/hypr-colors.lua.tmpl
-- Carregado pelo hyprland.lua via require("colors")
hl.config({
    general = {
        ["col.active_border"]   = "rgb({{accent_raw}})",
        ["col.inactive_border"] = "rgb({{line_raw}})",
    },
})
