# Lido pelo systemd --user no inicio da sessao. Sem isto, servicos como o
# waybar nao enxergam ~/.scripts e todo botao que chama script falha em
# silencio — o clique simplesmente nao faz nada.
PATH=$HOME/.scripts:$HOME/.local/bin:$PATH
